// config/entorno.js

const source = 'api';
export const proveedorClientes = source;
export const proveedorEquipos = source; // 'local' o 'api'
export const proveedorOrdenes = source; // 'local' o 'api'
